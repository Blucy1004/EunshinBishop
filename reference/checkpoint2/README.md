# Checkpoint 2 accepted baseline

Frozen on 2026-07-18 after the project owner approved Checkpoint 2 for its
documented structural scope.

This directory is immutable migration evidence.  Do not rebuild into it or
replace its files.  `EunshinBishop_Q_Checkpoint2_source.zip` contains the exact
accepted `CMakeLists.txt`, project README, `src/`, `tests/`, `docs/`, and
network provenance documents.  The network weights are not duplicated in the
archive; their accepted project copy is identified below by hash.

Accepted verification:

```text
Release  519108 checks PASS
Debug    519108 checks PASS
startpos perft(1..5) 20, 400, 8902, 197281, 4865609
```

Artifact identities are recorded in `SHA256SUMS` and in
`docs/Checkpoint2_Report.md` inside the archive.

